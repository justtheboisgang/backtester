# Kopiere diese Datei zu ".env" und trage deinen echten Key ein.
#   cp .env.example .env
# Die ".env" ist in .gitignore -> landet NIE im Repo.
# Den Key bekommst du bei https://www.cryptohftdata.com/ (kostenloses Konto moeglich).

CRYPTOHFTDATA_API_KEY=hier_deinen_key_eintragen
